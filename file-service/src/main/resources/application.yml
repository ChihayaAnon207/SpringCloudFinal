spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 20MB

# MinIO 配置
minio:
  endpoint: ${MINIO_ENDPOINT:http://10.13.169.254:9000}
  access-key: ${MINIO_ACCESS_KEY:minioadmin}
  secret-key: ${MINIO_SECRET_KEY:minioadmin123}
  bucket-name: cloudblog

# Sentinel 配置
sentinel:
  transport:
    dashboard: localhost:8858
    port: 8722
